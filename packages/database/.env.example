DATABASE_URL="postgresql://point:point@localhost:5432/point?schema=public"
# Seed (`npm run db:seed`) bu URL’ye yazar. `apps/api/.env` içindeki DATABASE_URL ile aynı olmalı; aksi halde demo girişleri API’de çalışmaz.
